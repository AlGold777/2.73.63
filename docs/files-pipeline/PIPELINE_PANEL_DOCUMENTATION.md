# Pipeline Panel v10 — Полная функциональная документация

## Оглавление
1. [Общая архитектура](#1-общая-архитектура)
2. [Структура данных](#2-структура-данных)
3. [HTML-элементы](#3-html-элементы)
4. [CSS-классы и стили](#4-css-классы-и-стили)
5. [JavaScript функции](#5-javascript-функции)
6. [Логика коннекторов (стрелок)](#6-логика-коннекторов-стрелок)
7. [Синхронизация состояний](#7-синхронизация-состояний)
8. [Сценарии использования](#8-сценарии-использования)

---

## 1. Общая архитектура

### 1.1 Концепция Pipeline
Pipeline — визуальный конструктор цепочки обработки запросов через несколько LLM-моделей с возможностью многораундовой оценки (Judge).

### 1.2 Базовые структуры

**Минимальный Pipeline (roundCounter = 1):**
```
In → Models → Results → Output
```

**Стандартный Pipeline (roundCounter = 2):**
```
In → Models → Responses → Judge → Results → Output
```

**Расширенный Pipeline (roundCounter = 3+):**
```
In → Models → Responses → Judge → Responses → Judge → ... → Results → Output
```

### 1.3 Ключевые правила
- **Results** — всегда ОДИН, всегда перед Output
- **Responses** — появляется после Models и после каждого Judge (кроме последнего)
- После последнего Judge идёт сразу **Results**
- Кнопка "+" добавляет пару **Responses + Judge** между последним Judge и Results

### 1.4 Глобальные переменные
```javascript
const BLOCK_H = 58;      // Высота блока (52px) + отступ (6px)
const BLOCK_CENTER = 26; // Центр блока по вертикали (52/2)
let roundCounter = 2;    // Номер последнего раунда Judge
```

---

## 2. Структура данных

### 2.1 Идентификаторы элементов

| Элемент | ID паттерн | Пример |
|---------|------------|--------|
| Models R1 | `r1-models` | Единственный |
| Judge Rn | `r{n}-models` | `r2-models`, `r3-models` |
| Responses R1 | `resp1-stack` | После Models |
| Responses Rn | `resp{n}-stack` | `resp2-stack` (после Judge R2) |
| Results | `results-stack` | Единственный |
| Output | `output-stack` | Единственный |

### 2.2 Идентификаторы SVG-коннекторов

| Связь | ID паттерн | Пример |
|-------|------------|--------|
| In → Models | `svg-in-models` | Единственный |
| Models → Responses R1 | `svg-r1-resp1` | Единственный |
| Responses R1 → Judge R2 | `svg-resp1-r2` | Единственный |
| Judge Rn → Responses Rn | `svg-r{n}-resp{n}` | `svg-r2-resp2` |
| Responses Rn → Judge R(n+1) | `svg-resp{n}-r{n+1}` | `svg-resp2-r3` |
| Last Judge → Results | `svg-judge-results` | Единственный |
| Models → Results (minimal) | `svg-models-results` | При roundCounter=1 |
| Results → Output | `svg-results-out` | Единственный |

### 2.3 Атрибуты data-*

| Атрибут | Применение | Назначение |
|---------|------------|------------|
| `data-index` | Все блоки | Порядковый номер (0-3) |
| `data-round` | Динамические элементы | Номер раунда для удаления |
| `data-name` | Pipeline items | Имя pipeline для выбора |

---

## 3. HTML-элементы

### 3.1 Панель (pipeline-panel)
```
┌─────────────────────────────────────────────────────────┐
│ HEADER: Pipeline / "Research & Analysis" │ 🗑️ 💾 ▶ Run │
├─────────────────────────────────────────────────────────┤
│ CANVAS: [In] → [Models] → ... → [Output]    [+] [-]    │
├─────────────────────────────────────────────────────────┤
│ LIST: ▾ Pipelines [+]                                   │
│       ○ Research & Analysis [last]                      │
│       ○ Idea Validation                                 │
└─────────────────────────────────────────────────────────┘
```

### 3.2 Entry Point (Вход)
```html
<div class="stage-column">
    <div class="stage-label">In</div>
    <div class="entry-wrapper">
        <div class="entry-point" id="entryPoint">▶</div>
    </div>
</div>
```
- **Поведение**: Динамически позиционируется по центру активных моделей
- **Стиль**: Синий круг 18×18px

### 3.3 Model Block (Блок модели)
```html
<div class="model-block active" data-index="0">
    <div class="model-header">
        <input type="checkbox" class="model-checkbox" checked onchange="updateAll()">
        <span class="model-name">Claude</span>
    </div>
    <!-- Только для Judge: -->
    <select class="role-selector">
        <option>Synthesis</option>
        <option>Critic</option>
        <option>Meta</option>
        <option>Advocate</option>
    </select>
</div>
```

**Состояния:**
| Класс | Визуал | Условие |
|-------|--------|---------|
| `active` | Синяя рамка, подсветка | checkbox checked |
| `inactive` | Серая рамка, opacity 0.5 | checkbox unchecked |

**Размеры:** 110×52px, margin-bottom: 6px

### 3.4 Collector Row (Responses / Results)
```html
<div class="collector-row" data-index="0">
    <div class="status-indicator"></div>
    <span class="collector-model-name">Claude</span>
    <input type="checkbox" class="collector-checkbox" checked onchange="updateAll()">
</div>
```

**Типы:**
| Тип | CSS класс родителя | Цвет рамки |
|-----|-------------------|------------|
| Responses | — | Фиолетовый (#8b5cf6) |
| Results | `.results-column` | Зелёный (#22c55e) |

**Состояния:**
| Класс | Визуал | Условие |
|-------|--------|---------|
| (none) | Цветная рамка | Активен |
| `inactive` | Серая рамка, opacity 0.5 | Отключён |

**Размеры:** 100×52px, margin-bottom: 6px

### 3.5 Output Block
```html
<div class="output-block" data-index="0">
    <div class="output-header">
        <input type="checkbox" class="output-checkbox" checked onchange="updateAll()">
        <span class="output-icon">📝</span>
        <span class="output-name">Notes</span>
    </div>
    <div class="output-desc">Save to DATA</div>
</div>
```

**Варианты:**
| Index | Icon | Name | Description |
|-------|------|------|-------------|
| 0 | 📝 | Notes | Save to DATA |
| 1 | 📤 | Export | MD / JSON |
| 2 | 📋 | Clipboard | Copy |

**Размеры:** 100×52px

### 3.6 Connector Group (SVG контейнер)
```html
<div class="connector-group">
    <svg class="connector-svg" id="svg-xxx-yyy"></svg>
</div>
```
- **Размеры SVG:** 50×240px
- **padding-top:** 26px (выравнивание с stage-label)

### 3.7 Кнопки управления раундами
```html
<div class="round-buttons">
    <button class="add-round-btn" onclick="addRound()">+</button>
    <button class="remove-round-btn" id="removeRoundBtn" onclick="removeLastRound()">−</button>
</div>
```
- **"+"**: Всегда видна
- **"−"**: `display: none` при roundCounter ≤ 2, `display: flex` при roundCounter > 2

### 3.8 Список Pipelines
```html
<div class="pipeline-list">
    <div class="pipeline-list-header">
        <h3>▾ Pipelines</h3>
        <button class="add-pipeline-btn" onclick="addNewPipeline()">+</button>
    </div>
    <div class="pipeline-items" id="pipelineItems">
        <div class="pipeline-item active" data-name="Research & Analysis">
            <input type="radio" name="pipeline" class="pipeline-radio" checked>
            <span class="pipeline-item-name">Research & Analysis</span>
            <span class="last-badge">last</span>
        </div>
        <!-- ... -->
    </div>
</div>
```

---

## 4. CSS-классы и стили

### 4.1 Цветовая схема

| Элемент | Цвет | HEX |
|---------|------|-----|
| Активная рамка (Models/Judge) | Синий | #3b82f6 |
| Responses рамка | Фиолетовый | #8b5cf6 |
| Results/Output рамка | Зелёный | #22c55e |
| Неактивная рамка | Серый | #27272a |
| Brain-flow стрелки | Оранжевый | #fb923c |
| Active стрелки | Голубой | #60a5fa |
| Фон панели | Тёмно-синий градиент | #1a1a2e → #16213e |

### 4.2 Классы коннекторов

| Класс | Применение | Цвет |
|-------|------------|------|
| `.connector-line` | Базовая линия | Серый |
| `.connector-line.active` | Данные между коллекторами | Голубой |
| `.connector-line.brain-flow` | Выход модели | Оранжевый |
| `.merge-line` | Вертикальные линии слияния | Наследует цвет |
| `.arrow-head` | Наконечник стрелки | Наследует цвет |
| `.flow-anim` | Анимация потока | Пунктир, движение |

### 4.3 Анимации
```css
@keyframes flowAnim {
    to { stroke-dashoffset: -10; }
}
.flow-anim {
    stroke-dasharray: 6 4;
    animation: flowAnim 0.6s linear infinite;
}
```

---

## 5. JavaScript функции

### 5.1 Вспомогательные функции

#### `getBlockCenterY(index)`
```javascript
function getBlockCenterY(index) {
    return index * BLOCK_H + BLOCK_CENTER;
    // index=0: 26px, index=1: 84px, index=2: 142px, index=3: 200px
}
```
**Назначение:** Вычисляет Y-координату центра блока по его индексу.

#### `getCheckedIndices(stackId)`
```javascript
function getCheckedIndices(stackId) {
    // Возвращает индексы всех checked чекбоксов
    // Используется для Responses и Results
}
```
**Назначение:** Получает индексы отмеченных элементов в коллекторе (независимо от disabled).

#### `getActiveModelIndices(stackId)`
```javascript
function getActiveModelIndices(stackId) {
    // Возвращает индексы checked && !disabled чекбоксов
    // Используется для Models и Judge
}
```
**Назначение:** Получает индексы активных моделей (отмечены И не отключены).

#### `getOutputCheckedIndices(stackId)`
```javascript
function getOutputCheckedIndices(stackId) {
    // Специально для output-stack
    // Ищет .output-checkbox вместо обычных чекбоксов
}
```
**Назначение:** Получает индексы выбранных output-блоков.

### 5.2 Функции отрисовки коннекторов

#### `drawInputSplitConnector(svgId, targetStackId, colorClass)`
```
▶ ────┬── [Model 0]
      ├── [Model 1]
      └── [Model 2]
```
**Логика:**
1. Получить активные индексы целевых моделей
2. Вычислить Y-координаты всех целей
3. Найти min, max, mid
4. Позиционировать entry-point на mid
5. Нарисовать: горизонтальную линию → вертикальную → линии к целям со стрелками

#### `drawHorizontalConnectors(svgId, stackId, colorClass)`
```
[Model 0] ────→
[Model 1] ────→
[Model 2] ────→
```
**Логика:**
1. Получить активные индексы моделей
2. Для каждого: линия от 0 до W-6, стрелка на конце

#### `drawMergeSplitConnector(svgId, sourceStackId, targetStackId, colorClass)`
```
[Source 0] ──┐              ┌── [Target 0]
[Source 1] ──┼──────────────┼── [Target 1]
[Source 2] ──┘              └── [Target 2]
```
**Логика:**
1. Определить тип источника (collector vs model)
2. Определить тип цели (collector vs model vs output)
3. Выбрать функцию получения индексов для каждого
4. Вычислить координаты
5. Нарисовать: линии к merge → вертикаль merge → main line → вертикаль split → линии к целям

**Определение типа:**
```javascript
const isSourceCollector = /^(resp|results)/.test(sourceStackId);
const isTargetOutput = targetStackId === 'output-stack';
const isTargetCollector = /^(resp|results)/.test(targetStackId);
```

### 5.3 Функция синхронизации

#### `syncCollectorWithModels(modelsStackId, collectorStackId)`
**Назначение:** Синхронизирует состояние коллектора с моделями.

**Правила:**
| Модель checked | Коллектор результат |
|----------------|---------------------|
| ✓ | remove `.inactive`, disabled=false, если был disabled → checked=true |
| ✗ | add `.inactive`, disabled=true, checked=false |

### 5.4 Главная функция обновления

#### `updateAll()`
**Вызывается:** При любом изменении чекбокса (onchange).

**Порядок выполнения:**
1. **Обновить визуал model-блоков** — active/inactive классы, role-selector disabled
2. **Синхронизировать коллекторы:**
   - Если roundCounter=1: Models → Results
   - Иначе:
     - Models → Responses R1
     - Для r от 2 до roundCounter-1: Judge Rr → Responses Rr
     - Judge (last) → Results
3. **Обновить output-блоки** — inactive класс по checkbox
4. **Отрисовать все коннекторы:**
   - svg-in-models
   - Если roundCounter=1: svg-models-results
   - Иначе: вся цепочка
   - svg-results-out

### 5.5 Управление раундами

#### `addRound()`
**При roundCounter = 1 (минимальный шаблон):**
1. roundCounter = 2
2. Переименовать svg-models-results → svg-r1-resp1
3. Добавить HTML: Responses R1 + Connector + Judge R2
4. Показать кнопку "−"

**При roundCounter ≥ 2:**
1. currentLastJudge = roundCounter
2. roundCounter++
3. Добавить HTML: Connector + Responses + Connector + Judge
4. Вставить перед insertionPoint

**Важно:** Новые Responses создаются со ВСЕМИ строками active/checked — синхронизация происходит в updateAll().

#### `removeLastRound()`
**При roundCounter = 2:**
1. Удалить все элементы с data-round="2"
2. Удалить Responses R1
3. Удалить связанные коннекторы
4. Переименовать svg-judge-results → svg-models-results
5. roundCounter = 1
6. Скрыть кнопку "−"

**При roundCounter > 2:**
1. Удалить все элементы с data-round="{roundCounter}"
2. roundCounter--

### 5.6 Управление Pipelines

#### `addNewPipeline()`
1. Запросить имя
2. Вызвать resetToMinimalTemplate()
3. Добавить в список
4. Обновить заголовок

#### `resetToMinimalTemplate()`
1. Удалить все раунды ≥ 2
2. Удалить Responses R1 и связанные коннекторы
3. Переименовать коннектор к Results
4. roundCounter = 1

#### `savePipeline()`
1. Запросить имя (по умолчанию текущее)
2. Добавить в список

#### `addPipelineToList(name)`
1. Проверить существование
2. Убрать last-badge у других
3. Создать новый элемент
4. Добавить обработчик клика

#### `selectPipeline(item)`
1. Убрать active у всех
2. Добавить active выбранному
3. Обновить заголовок

---

## 6. Логика коннекторов (стрелок)

### 6.1 Типы коннекторов

| Тип | Функция | Применение | Цвет |
|-----|---------|------------|------|
| Input Split | drawInputSplitConnector | In → Models | active (голубой) |
| Horizontal | drawHorizontalConnectors | Models/Judge → Responses/Results | brain-flow (оранжевый) |
| Merge-Split | drawMergeSplitConnector | Responses → Judge, Results → Output | active (голубой) |

### 6.2 Правила определения источника индексов

| Stack ID | Функция | Примечание |
|----------|---------|------------|
| `r*-models` | getActiveModelIndices | checked && !disabled |
| `resp*-stack` | getCheckedIndices | только checked |
| `results-stack` | getCheckedIndices | только checked |
| `output-stack` | getOutputCheckedIndices | .output-checkbox |

### 6.3 Координатная система SVG

```
SVG viewBox: 0 0 50 240

X координаты:
- 0: левый край (начало линий)
- 15 (mergeX): точка слияния
- 35 (splitX = W-15): точка разветвления
- 44 (W-6): конец линии перед стрелкой
- 50 (W): правый край

Y координаты (для 4 блоков):
- index 0: 26px (центр)
- index 1: 84px
- index 2: 142px
- index 3: 200px
```

### 6.4 Структура стрелки (arrow-head)
```html
<polygon points="44,{y-4} 50,{y} 44,{y+4}"/>
```
Треугольник 6×8px, направленный вправо.

---

## 7. Синхронизация состояний

### 7.1 Каскад синхронизации

```
Models (R1)
    │
    ├──→ Responses R1
    │        │
    │        └──→ Judge R2
    │                │
    │                ├──→ Responses R2 (если есть R3)
    │                │        │
    │                │        └──→ Judge R3
    │                │                │
    │                │                └──→ ...
    │                │
    │                └──→ Results (если последний Judge)
    │
    └──→ Results (если roundCounter=1)
            │
            └──→ Output (независим)
```

### 7.2 Правила синхронизации

**Models → Responses:**
- Модель включена → строка Responses активна, checkbox enabled
- Модель выключена → строка inactive, checkbox disabled + unchecked

**Judge → Responses (следующие):**
- Аналогично Models → Responses

**Judge (последний) → Results:**
- Аналогично Models → Responses

**Results → Output:**
- Нет синхронизации, Output независим
- Стрелки рисуются на основе checked чекбоксов обоих

### 7.3 Автоматическое включение
При активации ранее отключённой модели, соответствующая строка коллектора:
1. Становится активной
2. Checkbox становится enabled
3. Checkbox автоматически checked (если был disabled)

---

## 8. Сценарии использования

### 8.1 Создание нового Pipeline
1. Нажать "+" рядом с "Pipelines"
2. Ввести имя
3. Получить минимальный шаблон: Models → Results → Output
4. Добавить раунды через "+" на canvas

### 8.2 Добавление Judge
1. Нажать "+" на canvas
2. Если минимальный: появится Responses + Judge R2
3. Если есть Judge: появится Responses + новый Judge

### 8.3 Удаление Judge
1. Нажать "−" на canvas
2. Удалится последняя пара Responses + Judge
3. При удалении до R2 → возврат к минимальному шаблону

### 8.4 Выбор моделей
1. Отметить/снять чекбокс модели
2. Автоматически обновятся:
   - Визуал блока (active/inactive)
   - Соответствующие коллекторы
   - Все стрелки

### 8.5 Фильтрация результатов
1. В любом Responses/Results снять чекбокс
2. Стрелка к следующему этапу исчезнет
3. Модель не попадёт в следующий раунд

### 8.6 Выбор выходов
1. Отметить нужные Output (Notes, Export, Clipboard)
2. Стрелки от Results перестроятся

---

## Приложение A: Полный список элементов по ID

### Статические элементы
| ID | Тип | Описание |
|----|-----|----------|
| `entryPoint` | div | Точка входа (▶) |
| `entryWrapper` | div | Контейнер для позиционирования entry |
| `round1` | div.stage-column | Колонка R1 Models |
| `r1-models` | div.model-stack | Стек моделей R1 |
| `responses1` | div.collector-column | Колонка Responses R1 |
| `resp1-stack` | div.collector-stack | Стек Responses R1 |
| `round2` | div.stage-column | Колонка R2 Judge |
| `r2-models` | div.model-stack | Стек моделей R2 Judge |
| `insertionPoint` | div | Точка вставки новых раундов |
| `connectorToResults` | div.connector-group | Контейнер коннектора к Results |
| `resultsColumn` | div.collector-column | Колонка Results |
| `results-stack` | div.collector-stack | Стек Results |
| `connectorToOutput` | div.connector-group | Контейнер коннектора к Output |
| `outputColumn` | div.stage-column | Колонка Output |
| `output-stack` | div.output-stack | Стек Output |
| `removeRoundBtn` | button | Кнопка удаления раунда |
| `currentPipelineName` | div | Название текущего pipeline |
| `pipelineItems` | div | Контейнер списка pipelines |

### Динамические элементы (создаются при addRound)
| ID паттерн | Тип | Описание |
|------------|-----|----------|
| `round{n}` | div.stage-column | Колонка Judge Rn |
| `r{n}-models` | div.model-stack | Стек моделей Judge Rn |
| `responses{n}` | div.collector-column | Колонка Responses Rn |
| `resp{n}-stack` | div.collector-stack | Стек Responses Rn |

### SVG коннекторы
| ID | Связь |
|----|-------|
| `svg-in-models` | In → Models |
| `svg-r1-resp1` | Models → Responses R1 |
| `svg-resp1-r2` | Responses R1 → Judge R2 |
| `svg-r{n}-resp{n}` | Judge Rn → Responses Rn |
| `svg-resp{n}-r{n+1}` | Responses Rn → Judge R(n+1) |
| `svg-judge-results` | Last Judge → Results |
| `svg-models-results` | Models → Results (minimal) |
| `svg-results-out` | Results → Output |

---

## Приложение B: Контрольный список для отладки

### При проблемах со стрелками:
- [ ] Проверить ID SVG элемента
- [ ] Проверить ID source и target стеков
- [ ] Убедиться что функция получения индексов правильная
- [ ] Проверить что элементы существуют в DOM

### При проблемах с синхронизацией:
- [ ] Проверить вызывается ли updateAll()
- [ ] Проверить roundCounter
- [ ] Проверить ID коллектора в syncCollectorWithModels
- [ ] Убедиться что шаблон создаётся со всеми строками active

### При проблемах с добавлением/удалением:
- [ ] Проверить data-round атрибуты
- [ ] Проверить insertionPoint позицию
- [ ] Проверить переименование SVG ID

---

*Документация для Pipeline Panel v10*
*Последнее обновление: Декабрь 2024*
